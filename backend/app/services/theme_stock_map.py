"""테마 → 소속 종목 매핑 (08-stock-etf-recommend §5).

⚠️ 현재는 큐레이션 매핑(대표 종목, 실제 KRX 코드). 실연동 시 ETF 구성종목 역설계 +
   네이버 금융 테마 스크래핑으로 자동 생성/확장. 매핑 없는 테마는 점수 placeholder 폴백.
"""
from __future__ import annotations

# {테마명: [(종목코드, 종목명), ...]}  대표 종목 위주(5축 집계 표본)
THEME_STOCKS: dict[str, list[tuple[str, str]]] = {
    "AI반도체·HBM": [("005930", "삼성전자"), ("000660", "SK하이닉스"), ("042700", "한미반도체"), ("039030", "이오테크닉스")],
    "원자력·SMR": [("034020", "두산에너빌리티"), ("052690", "한전기술"), ("083650", "비에이치아이"), ("105840", "우진")],
    "방산": [("012450", "한화에어로스페이스"), ("079550", "LIG넥스원"), ("064350", "현대로템"), ("047810", "한국항공우주")],
    "로봇": [("454910", "두산로보틱스"), ("277810", "레인보우로보틱스"), ("058610", "에스피지"), ("090360", "로보스타")],
    "조선": [("009540", "HD한국조선해양"), ("010140", "삼성중공업"), ("042660", "한화오션"), ("329180", "HD현대중공업")],
    "전력기기·전력망": [("267260", "HD현대일렉트릭"), ("010120", "LS ELECTRIC"), ("298040", "효성중공업"), ("001440", "대한전선")],
    "2차전지": [("373220", "LG에너지솔루션"), ("006400", "삼성SDI"), ("247540", "에코프로비엠"), ("003670", "포스코퓨처엠")],
    "바이오·제약": [("207940", "삼성바이오로직스"), ("068270", "셀트리온"), ("000100", "유한양행"), ("128940", "한미약품")],
    "우주항공·UAM": [("047810", "한국항공우주"), ("272210", "한화시스템"), ("274090", "켄코아에어로스페이스")],
    "엔터·미디어": [("352820", "하이브"), ("035900", "JYP Ent."), ("041510", "에스엠"), ("122870", "와이지엔터테인먼트")],
    "자동차부품": [("012330", "현대모비스"), ("018880", "한온시스템"), ("204320", "HL만도"), ("011210", "현대위아")],
    "화장품·뷰티": [("090430", "아모레퍼시픽"), ("051900", "LG생활건강"), ("192820", "코스맥스"), ("161890", "한국콜마")],
    "반도체장비": [("240810", "원익IPS"), ("036930", "주성엔지니어링"), ("319660", "피에스케이")],
    "파운드리": [("005930", "삼성전자"), ("000660", "SK하이닉스"), ("083310", "엘오티베큠")],
    "전력반도체": [("097520", "엠씨넥스"), ("357780", "솔브레인"), ("166090", "하나머티리얼즈")],
    "태양광": [("009830", "한화솔루션"), ("112610", "씨에스윈드"), ("322000", "현대에너지솔루션")],
    "바이오시밀러": [("068270", "셀트리온"), ("207940", "삼성바이오로직스"), ("298380", "에이비엘바이오")],
    "비만치료제": [("128940", "한미약품"), ("000100", "유한양행"), ("145720", "덴티움")],
    "게임": [("036570", "엔씨소프트"), ("251270", "넷마블"), ("259960", "크래프톤"), ("293490", "카카오게임즈")],
    "조선기자재": [("100090", "삼강엠앤티"), ("009540", "HD한국조선해양"), ("075580", "세진중공업")],
}


def _normalize(s: str) -> str:
    """테마명 정규화 — 공백·특수문자 제거, 소문자화 (매칭용)."""
    import re
    return re.sub(r"[^0-9a-z가-힣]", "", s.lower())


def _match_collected(theme_name: str, collected: dict) -> list[list[str]] | None:
    """수집 맵에서 테마명 매칭 — 정확 일치 → 부분 토큰 일치."""
    if theme_name in collected:
        return collected[theme_name]
    norm = _normalize(theme_name)
    if not norm:
        return None
    # 우리 테마명의 핵심 토큰(2자+)이 수집 테마명에 포함되거나 그 반대
    for key, members in collected.items():
        nk = _normalize(key)
        if norm == nk or (len(norm) >= 2 and (norm in nk or nk in norm)):
            return members
    return None


def get_member_stocks(theme_name: str, limit: int = 4) -> list[tuple[str, str]]:
    """테마 소속 종목(코드,이름) 반환.

    우선순위: 네이버 수집 매핑(역설계) → 큐레이션 매핑 → 빈 리스트.
    """
    # 지연 임포트(순환 방지) + 캐시 로드
    try:
        from app.services.collectors.theme_mapping_collector import load_cached_map
        collected = load_cached_map()
    except Exception:
        collected = {}
    if collected:
        hit = _match_collected(theme_name, collected)
        if hit:
            return [(m[0], m[1]) for m in hit[:limit]]
    return THEME_STOCKS.get(theme_name, [])[:limit]
